import React from 'react';
import { Link } from 'react-router-dom';

const Login = (props) => {
  console.log(props);
  return (
     <div>
       Login page
    </div>
  );
};

export default Login;
